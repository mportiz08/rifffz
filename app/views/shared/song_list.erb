<div id="player">
  <div class="top-bar">
    <div class="container">
      <div class="row">
        <div class="span3 controls">
          <ul class="unstyled">
            <li>
              <a href="#" class="prev-track sprite-icons-Back"></a>
            </li>
            <li>
              <a href="#" class="play-pause sprite-icons-Play"></a>
            </li>
            <li>
              <a href="#" class="next-track sprite-icons-Next"></a>
            </li>
          </ul>
        </div>
        <div class="span3 controls">
          <ul class="unstyled">
            <li>
              <a href="/" class="sprite-icons-Home"></a>
            </li>
            <li>
              <a href="#" class="sprite-icons-Search"></a>
            </li>
          </ul>
        </div>
        <div class="span1">
          <div class="time-passed">0:00</div>
        </div>
        <div class="span7 progress">
          <div class="progress-bar">
            <a href="#"><span style="width: 0%;"></span></a>
          </div>
        </div>
        <div class="span2">
          <div class="time-total">0:00</div>
        </div>
        <audio>
          <source src="<%= @song_list.songs.first.audio_url %>" />
        </audio>
      </div>
    </div>
  </div>
  <div id="main" class="container">
    <div class="row">
      <div class="album-cover-view span8">
        <div class="album-cover" style="background-image: url('<%= @song_list.cover_url %>'); background-size: contain;">
          <div class="album-cover-overlay"></div>
        </div>
      </div>
      <div class="album-info span8">
        <h1 class="album-name"><%= @song_list.title %></h1>
        <% if @song_list.respond_to?(:new?) && @song_list.new? %><span class="label important">new</span><% end %>
        <% if @song_list.respond_to?(:year) %><small class="album-year"><%= @song_list.year %></small><% end %>
        <% if @song_list.respond_to?(:artist) %><h2 class="artist-name"><%= @song_list.artist.name %></h2><% end %>
        <ul class="album-song-list unstyled">
          <% @song_list.songs.each do |song| %>
              <% if song == @song_list.songs.first %>
                <li data-song-slug="<%= song.slug %>" class="now-playing">
                  <span class="label success">now playing</span>
                  <a href="#"><%= song.title %></a>
                  <% unless @song_list.respond_to?(:artist) %><br /><small><%= song.album.artist.name %></small><% end %>
                </li>
              <% else %>
                <li data-song-slug="<%= song.slug %>">
                  <a href="#"><%= song.title %></a>
                  <% unless @song_list.respond_to?(:artist) %><br /><small><%= song.album.artist.name %></small><% end %>
                </li>
              <% end %>
          <% end %>
        </ul>
      </div>
    </div>
  </div>
</div>
